#include <stdio.h>
#include <string.h>

int main()
{
    /*
        // for taking input by "gets".
        char ar[18];
        gets(ar);
        printf("%s", ar);
    */
    /*
        // for taking input by "fgets".
        char ar[18];
        fgets(ar, 18, stdin);
        printf("%s", ar);
    */
    /*
        // for taking input by "scanf()".
        char ar[18];
        scanf("%s", &ar);
        printf("%s", ar);
    */
    /*
        // find the length of a string
        char ar[18];
        scanf("%s", &ar);
        int index = 0;
        while(ar[index]!='\0')
        {
            index++;
        }
        printf("%s", ar);
    */
    /*
         // find the length by "strlen()"
         char ar[18];
         scanf("%s", &ar);
         int length = strlen(ar);
         printf("%d", length);
     */
    /*
         // use of "strcpy()" function
         char a[100], b[100];
         scanf("%s %s", &a, &b);
         strcpy(a, b);
         printf("%s %s", a, b);
     */
    /*
         // use of "strcat()" function
         char a[100], b[100];
         scanf("%s %s", &a, &b);
         strcat(a, b);
         printf("%s %s", a, b);
     */
    /*
         // use of "strcmp()" function
         char a[100], b[100];
         scanf("%s %s", &a, &b);
         int val = strcmp(a, b);
         printf("%d", val);
     */

    return 0;
}